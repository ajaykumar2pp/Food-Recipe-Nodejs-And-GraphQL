// const typeDefs =`#graphql

// type Recipe{
//     title: String
//     price:Int
// }

// type Query{
//     test : String
//     sayHello(name:String):String
// }


// `;

// module.exports ={typeDefs}

// The underscore (_) is commonly used as a placeholder in function or method parameters .it's used to indicate that you're not going to use the first parameter._ is a placeholder for the first argument, which is often the parent object when dealing with nested resolvers.you may not need the first parameter (_) and use it as a convention to indicate that it's intentionally not used in the function. It's essentially a way of telling the reader of the code that the first parameter is ignored.


// args is an object that contains the arguments provided in the GraphQL query or mutation.